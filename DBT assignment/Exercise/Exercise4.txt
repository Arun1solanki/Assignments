Exercise 4:
1. Display the minimum Status in the Supplier table.
select min(status) from s;

2. Display the maximum Weight in the Parts table.

select max(weight) from p;

3. Display the average Weight of the Parts.
select avg(weight) from p;

select avg(weight) from p;

4. Display the total Quantity sold for part ‘P1’.

 select sum(QTY) from spj where pid='p1'

5. Display the total Quantity sold for each part.

select pid,sum(qty) from spj group by pid;

6. Display the average Quantity sold for each part.

select pid,avg(qty) from spj group by pid;

7. Display the maximum Quantity sold for each part, provided the maximum Quantity 
is greater than 800

 select pid,max(qty) from spj group by pid;

8. Display the Status and the count of Suppliers with that Status

select status,count(Sid) from s group by status;

9. Display the count of Projects going on in different cities.

select city ,count(sid) from s group by city;

10. What is the difference between COUNT(Status) and COUNT(*) ?

count(*) count all rows present in the table
while count(status) count non null values in status column
 
11. Display the Status and the Count of Suppliers with that Status in the following format 
as shown below:- 
 
Status Count 
Ten 1 
Twenty 2 
Thirty 3

 select case
    -> when status=10 then 'Ten'
    -> when status=20 then 'twenty'
    -> when status=30 then 'Thirty'
    -> when status=40 then 'Fourty'
    -> when status=50 then 'Fifty'
    -> else 'no status'
    -> end as status,
    -> count(status) from s group by status;

